const input = require("fs").readFileSync("/dev/stdin").toString().split("\n");
const N = Number(input[0]); // 문자열의 길이, 1 <= N <= 10_000
const S = input[1].trim();
let answer = 1;

for (let i = 1; i < N; i++) {
	if (S[i - 1] !== S[i]) {
		answer++;
	}
}

console.log(answer);