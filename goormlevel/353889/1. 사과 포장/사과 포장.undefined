const input = require("fs").readFileSync("/dev/stdin").toString();

// A: 가지고 있는 사과의 개수, 1 <= A <= 1_000_000_000
// B: 한 박스에 들어가야 하는 사과의 개수, 1 <= B <= 1_000_000_000
const [A, B] = input.split(" ").map(Number);

console.log(A % B);