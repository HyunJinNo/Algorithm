const input = require("fs").readFileSync("/dev/stdin").toString().split("\n");
const N = Number(input[0]); // 문자열의 개수, 1 <= N <= 1_000
let answer = 0;

for (let i = 1; i <= N; i++) {
	const S = input[i].trim();

	if (S.length === i) {
		answer++;
	}
}

console.log(answer);