const input = require("fs").readFileSync("/dev/stdin").toString();
const [a, b, c] = input.split(" ").map(Number);

if (a === b && b === c) {
	console.log(1);
} else if (a === b || a === c || b === c) {
	console.log(2);
} else {
	console.log(3);
}